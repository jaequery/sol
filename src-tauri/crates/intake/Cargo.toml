[package]
name = "solcut-intake"
description = "The machine-specific edges of an import: iCloud placeholders and photo formats only the system can read. No Tauri or GUI dependencies."
version.workspace = true
edition.workspace = true
license.workspace = true

[dependencies]
serde.workspace = true
thiserror.workspace = true
tokio.workspace = true

[dev-dependencies]
tokio = { version = "1", features = ["full"] }
